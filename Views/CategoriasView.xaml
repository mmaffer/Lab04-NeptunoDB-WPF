<UserControl x:Class="WpfApp1.Views.CategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Mantenimiento de Categorías"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,8" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Nuevo"     Command="{Binding NuevoCommand}"     Padding="10,4" Margin="0,0,6,0" />
            <Button Content="Guardar"   Command="{Binding GuardarCommand}"   Padding="10,4" Margin="0,0,6,0" />
            <Button Content="Eliminar"  Command="{Binding EliminarCommand}"  Padding="10,4" Margin="0,0,6,0" />
            <Button Content="Refrescar" Command="{Binding RefrescarCommand}" Padding="10,4" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Categorias}"
                      SelectedItem="{Binding Seleccionada}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"          Binding="{Binding CategoriaID}"     Width="50" />
                    <DataGridTextColumn Header="Nombre"      Binding="{Binding NombreCategoria}" Width="180" />
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}"     Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1" BorderBrush="#CCC" BorderThickness="1" Padding="10">
                <StackPanel>
                    <TextBlock Text="Datos de la categoría" FontWeight="Bold" Margin="0,0,0,8" />

                    <TextBlock Text="Nombre *" />
                    <TextBox Text="{Binding Edicion.NombreCategoria, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="Descripción" Margin="0,6,0,0" />
                    <TextBox Text="{Binding Edicion.Descripcion, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True" TextWrapping="Wrap" Height="80"
                             VerticalScrollBarVisibility="Auto" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="#EEE" Padding="6" Margin="0,8,0,0">
            <TextBlock Text="{Binding MensajeEstado}" TextWrapping="Wrap" />
        </Border>
    </Grid>
</UserControl>
